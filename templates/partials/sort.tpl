<div class="sort">
    <a href="{$baseUrl}?sort=date" class="{if $sort == 'date'}active{/if}">По дате</a>
    <a href="{$baseUrl}?sort=views" class="{if $sort == 'views'}active{/if}">По просмотрам</a>
</div>
